{
  "name": "desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "electrobun dev",
    "dev:hmr": "concurrently \"pnpm run hmr\" \"electrobun dev --watch\"",
    "hmr": "turbo run dev -F web",
    "build": "turbo run build -F web && electrobun build",
    "build:stable": "turbo run build -F web && electrobun build --env=stable",
    "build:canary": "turbo run build -F web && electrobun build --env=canary",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "electrobun": "^1.18.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/three": "^0.165.0",
    "concurrently": "^10.0.3",
    "typescript": "^7.0.2"
  }
}
